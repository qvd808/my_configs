## Fulfillment checklist (required in emit_answer.checklist)
1. behavior — what it does + important side effects
2. interface — params / returns / errors (from LSP or careful inference)
3. architecture — callers, callees, file role
4. user_ask — how you answered their specific request
5. gaps — anything unverified (optional but preferred when tools failed)
